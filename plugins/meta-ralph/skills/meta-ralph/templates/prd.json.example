{
  "project": "MyApp",
  "branchName": "ralph/task-priority",
  "description": "Task Priority System - Add priority levels to tasks",
  "runner": {
    "command": "claude",
    "args": ["-p", "{PROMPT}", "--dangerously-skip-permissions"]
  },
  "userStories": [
    {
      "id": "US-001",
      "title": "Add priority field to database",
      "description": "As a developer, I need to store task priority so it persists across sessions.",
      "acceptanceCriteria": [
        "Add priority column to tasks table: 'high' | 'medium' | 'low' (default 'medium')",
        "Generate and run migration successfully",
        "Typecheck passes"
      ],
      "priority": 1,
      "status": "todo",
      "notes": ""
    },
    {
      "id": "US-002",
      "title": "Expose priority in API response",
      "description": "As a frontend developer, I need the task list endpoint to include priority so the UI can sort by it.",
      "acceptanceCriteria": [
        "GET /api/tasks returns priority field for each task",
        "Existing API tests still pass",
        "Add new test asserting priority is present"
      ],
      "priority": 2,
      "status": "todo",
      "notes": ""
    }
  ]
}
